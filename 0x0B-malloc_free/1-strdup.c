#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup - returns a pointer to a new string
 * @str: string
 * Return: pointer
 */

char *_strdup(char *str)
{
	char *str;

	if (str == NULL)
	{
		return (NULL);
	}

	str = malloc(size of(char) * 3);
	str[0] = 'O';
	str[1] = 'K';
	str[2] = '\0';
	_putchar(str);

	return (0);
}
