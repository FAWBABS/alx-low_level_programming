#include <stdio.h>
/**
 * main - prints its name
 * @arcc: number of items in command line
 * @argv: array size of the command line
 * Return - void
 */
int main(int argc, char ** argv_)
{
	printf("%s", argv[0])
		return (0);
}
