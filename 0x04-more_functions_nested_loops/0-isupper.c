#include "main.h"
#include <stdio.h>

/**
 * isupper - checks for uppercase letter
 * @c: letter to be considered
 * Return: Always 0
 */

int _isupper(int c)
{
	char c;

	if (c == 'A')
		return (1);
	else
		return (0);
	_putchar('\n');
}
