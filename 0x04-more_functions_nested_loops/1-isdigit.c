#include "main.h"

/**
 * _isdigit - check for digits
 * @c: number to be considered
 * Return: Always 0
 */
int _isdigit(int c)
{
	c = 0
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
